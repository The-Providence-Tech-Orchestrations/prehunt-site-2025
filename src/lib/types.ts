export enum PuzzleDifficulty {
  Expert = "expert",
  Casual = "casual",
}

export enum PuzzlePacket {
  One = 1,
  Two = 2,
  SuperMeta = 3,
}

export enum PacketSlug {
  One = "packet-1",
  Two = "packet-2",
  SuperMeta = "supermeta",
}
